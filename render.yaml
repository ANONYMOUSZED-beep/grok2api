services:
  - type: web
    name: grok2api
    env: docker
    dockerfilePath: ./Dockerfile.render
    dockerContext: .
    plan: free
    healthCheckPath: /health
    envVars:
      - key: TZ
        value: Asia/Shanghai
